# Novastack Technology Website

A premium, responsive single-page website for Novastack Technology featuring a dark/light theme toggle, animated sections, floating particles, and contact options.

## Run locally

Open `index.html` in a browser, or use a simple local server such as:

```bash
python -m http.server
```
